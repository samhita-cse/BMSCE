{
  "name": "studyflow-student-app",
  "private": true,
  "workspaces": [
    "client",
    "functions"
  ],
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "lint": "npm --prefix client run lint",
    "functions:serve": "npm --prefix functions run serve"
  }
}
